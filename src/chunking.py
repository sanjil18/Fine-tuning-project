def chunk_text(text: str, chunk_size: int = 500) -> list[str]:
    """
    Split raw text into chunks for QA generation.

    Adapted from the RAG project's chunking logic.
    """
    # TODO: paste actual chunking logic from RAG project
    pass
